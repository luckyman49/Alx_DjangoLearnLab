How to run tests

Run the app tests (recommended):
python manage.py test relationship_app

Or run all tests (project discovery):
python manage.py test

Both commands use Django's test runner and will create a temporary test database.
